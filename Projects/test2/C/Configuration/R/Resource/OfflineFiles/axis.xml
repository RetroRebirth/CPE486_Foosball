<?xml version="1.0" encoding="UTF-8"?>

<axisInfo>
  <defaultAxis>
    <name></name>
    <number>0</number>
    <node>0</node>
  </defaultAxis>
  <axisList>
    <!-- The following is an example of a fully fleshed out axis structure -->
    <!--
      Most structures are optional within the configuration: only <name> and <number>
      are strictly required. All axes must also have a <node> or one of its substitutes
      (see below).

      The following structures can be substituted for <node>:

      <servoNetAxis>
        <node> 1 </node>
      </servoNetAxis>

      <servoNetExtEncoder>
        <node> 1 </node>
      </servoNetExtEncoder>

      <lioExtEncoder>
        <unit> 0 </unit>
        <resolution> 65536 </resolution>
        <invertPhaseA> false </invertPhaseA>
        <invertPhaseB> false </invertPhaseB>
        <invertIndexPulse> false </invertIndexPulse>
        <pulseCountMode> quadrature_x4 </pulseCountMode>
      </lioExtEncoder>

      <virtualAxis/>

      Note: Each of the sub-elements of <softLimits> (pos,vel,accel,decel,torque)
      can either contain a <symmetric>xxx.xxx</symmetric> structure OR a 
      <asymmetric><min>xxx.xxx</min><max>xxx.xxx</max></asymmetric> structure
      OR they can be empty, which means 'infinite' or no limits for that quantity.
      Examples are provided for each case.

      Note: <externalAxis> can be substituted for <axis>, though it is deprecated.
            For backward compatibility, such substitutions should be located at the
            end of the list, after all <axis> elements, and should contain <lioExtEncoder>
            or <servoNetExtEncoder> substitutions.
    -->
    <!--
    <axis>
      <name>MyAxis</name>
      <number>1</number>
      <node>1</node>
      <driveConversion>
        <posScale>1.0</posScale>
        <posOffset>0.0</posOffset>
        <torqueScale>1.0</torqueScale>
        <torqueOffset>0.0</torqueOffset>
      </driveConversion>
      <softLimits>
        <pos>
          <asymmetric>
            <min>-20.5</min>
            <max>1.275</max>
          </asymmetric>
        </pos>
        <vel>
          <symmetric>50</symmetric>
        </vel>
        <accel>
          <asymmetric>
            <min>-20</min>
            <max>50</max>
          </asymmetric>
        </accel>
        <decel>
          <asymmetric>
            <min>-50</min>
            <max>20</max>
          </asymmetric>
        </decel>
        <torque/>
      </softLimits>
      <continuousWrap>
        <range>
          <asymmetric>
            <min>0</min>
            <max>1</max>
          </asymmetric>
        </range>
      </continuousWrap>
      <filterList>
        <filter>
          <movingAverageFilter>
            <timeConstant>0.01</timeConstant>
            <source>position</source>
          </movingAverageFilter>
        </filter>
        <filter>
          <movingAverageFilter>
            <timeConstant>0.01</timeConstant>
            <source>position</source>
          </movingAverageFilter>
        </filter>
      </filterList>
      <enableTimeout>1000</enableTimeout>

    </axis>
    -->

  </axisList>
</axisInfo>

